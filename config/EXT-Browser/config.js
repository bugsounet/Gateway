var defaultConfig = {
  module: "EXT-Browser",
  disabled: false,
  config: {
    debug: false,
    displayDelay: 60 * 1000,
    scrollActivate: false,
    scrollStep: 25,
    scrollInterval: 1000,
    scrollStart: 5000
  }
}

var schema = {

}

exports.default = defaultConfig
exports.schema = schema
